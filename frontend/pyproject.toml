[project]
name = "web"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Bijesh Mohan", email = "bijeshmohankm@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "python-fasthtml>=0.12.28",
]

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"
